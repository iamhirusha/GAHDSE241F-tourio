<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tourio"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePageActivity" />

        <activity android:name=".SignUpActivity"
            android:exported="true"/>

        <activity
            android:name=".LoginPageActivity"
            android:exported="true"/>

        <activity android:name=".HotelSignFormActivity"/>


        <activity android:name=".HotelProfileActivity"/>

        <activity android:name=".HotelProfileUserViewActivity"/>

        <activity android:name=".HotelsPageActivity"/>

        <activity android:name=".PreDefinedToursFormActivity"/>

        <activity android:name=".UserToursRequestFormActivity"/>

        <activity android:name=".HomeNavBarActivity"/>

        <activity android:name=".BookTourActivity"/>

        <activity android:name=".PublishedRequestsPageActivity"/>

        <activity android:name=".HotelDashBoardActivity"/>

        <activity android:name=".MapsPageActivity"/>

        <activity android:name=".HotelTourBookingsActivity"/>

        <activity android:name=".OngoingTourActivity"/>


    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


</manifest>